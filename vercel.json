{
  "builds": [
    { "src": "server/index.js", "use": "@vercel/node" },
    { "src": "client/package.json", "use": "@vercel/static-build" }
  ],
  "routes": [
    { "src": "/api/(.*)", "build": "server/index.js" },
    { "src": "/(.*)", "build": "client/dist/$1" }
  ]
}